# Server Configuration
PORT=5000
NODE_ENV=development
# NODE_ENV=production

# Client URLs (switch between development and production)
# Development
CLIENT_URL=http://localhost:5173
# Production
# CLIENT_URL=https://your-production-client-url.com

# Server URLs (for reference)
# Development
SERVER_URL=http://localhost:5000
# Production
# SERVER_URL=https://your-production-server-url.com

# Database Configuration - Aiven MySQL
DB_HOST=your-db-host
DB_PORT=your-db-port
DB_USER=your-db-user
# Use a strong, randomly generated password for DB_PASSWORD. See https://bitwarden.com/password-generator/ or similar tools.
DB_PASSWORD=your-db-password
DB_NAME=your-db-name

# SSL Configuration
DB_SSL_CA_PATH=./ca-certificate.crt

# JWT Secret (use a strong, random string at least 32 characters long)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-min-32-chars-random-string

# To switch to production:
# 1. Change NODE_ENV to production
# 2. Uncomment production CLIENT_URL
# 3. Comment out development CLIENT_URL